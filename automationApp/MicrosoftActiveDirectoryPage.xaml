<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="automationApp.MicrosoftActiveDirectoryPage"
             BackgroundImageSource="fon.jpg"
             NavigationPage.HasNavigationBar="False">

    <ScrollView>
        <Grid Padding="24">
            <!-- Полупрозрачный фон -->
            <Frame
                BackgroundColor="#F2F2F2"
                BorderColor="Transparent"
                CornerRadius="32"
                Padding="0"
                HasShadow="False"
                Opacity="0.5"
                HorizontalOptions="Center"
                VerticalOptions="Fill"
                WidthRequest="1350" />

            <!-- Контент -->
            <Frame
                BackgroundColor="Transparent"
                BorderColor="Transparent"
                CornerRadius="32"
                Padding="16,16,16,16"
                HasShadow="False"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                WidthRequest="1300">
                <Grid Padding="24" RowSpacing="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <Label Text="Microsoft Active Directory"
                           Grid.Row="0"
                           FontSize="48"
                           FontAttributes="Bold"
                           FontFamily="Inter"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           TextColor="Black"
                           Margin="0,0,0,0" />

                    <!-- Заголовки таблиц -->
                    <Grid Grid.Row="1" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Label Text="Папка"
                               Grid.Column="0"
                               FontSize="28"
                               FontAttributes="Bold"
                               FontFamily="Inter"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               TextColor="Black"
                               Margin="0,0,0,0" />
                        <Label Text="Группа"
                               Grid.Column="1"
                               FontSize="28"
                               FontAttributes="Bold"
                               FontFamily="Inter"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               TextColor="Black"
                               Margin="0,0,0,0" />
                        <Label Text="Пользователи группы"
                               Grid.Column="2"
                               FontSize="28"
                               FontAttributes="Bold"
                               FontFamily="Inter"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               TextColor="Black"
                               Margin="0,0,0,0" />
                        <Label Text="Все пользователи"
                               Grid.Column="3"
                               FontSize="28"
                               FontAttributes="Bold"
                               FontFamily="Inter"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               TextColor="Black"
                               Margin="0,0,0,0" />
                    </Grid>

                    <!-- Таблицы -->
                    <Grid Grid.Row="2" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <CollectionView x:Name="CVRootFolders" Grid.Column="0" HeightRequest="200" SelectionMode="Single" SelectionChanged="CVRootFolders_SelectionChanged">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding}" BackgroundColor="#E0E0E0" TextColor="Black" Margin="5"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <CollectionView x:Name="CVSubFolders" Grid.Column="1" HeightRequest="200" SelectionMode="Single" SelectionChanged="CVSubFolders_SelectionChanged">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding}" BackgroundColor="#E0E0E0" TextColor="Black" Margin="5"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <CollectionView x:Name="CVGroupUsers" Grid.Column="2" HeightRequest="200" SelectionMode="Single" SelectionChanged="OnGroupUsersSelected">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding}" BackgroundColor="#E0E0E0" TextColor="Black" Margin="5"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <CollectionView x:Name="CVAllUsers" Grid.Column="3" HeightRequest="200" SelectionMode="Single" SelectionChanged="OnAllUsersSelected">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding}" BackgroundColor="#E0E0E0" TextColor="Black" Margin="5"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Grid>

                    <!-- Поля ввода + Кнопки -->
                    <Grid Grid.Row="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <HorizontalStackLayout Grid.Row="0" Spacing="20">
                            <Frame WidthRequest="326" HeightRequest="70" BackgroundColor="White" CornerRadius="32" Padding="32,0" HasShadow="False" BorderColor="#E0E0E0">
                                <Entry x:Name="UserNameEntry"
                                       Placeholder="Пользователь"
                                       FontSize="22"
                                       FontFamily="Inter"
                                       TextColor="Black"
                                       PlaceholderColor="#4A4A4A"
                                       VerticalOptions="Center"
                                       BackgroundColor="Transparent" />
                            </Frame>
                            <Frame WidthRequest="326" HeightRequest="70" BackgroundColor="White" CornerRadius="32" Padding="32,0" HasShadow="False" BorderColor="#E0E0E0">
                                <Entry x:Name="DescriptionEntry"
                                       Placeholder="Описание"
                                       FontSize="22"
                                       FontFamily="Inter"
                                       TextColor="Black"
                                       PlaceholderColor="#4A4A4A"
                                       VerticalOptions="Center"
                                       BackgroundColor="Transparent" />
                            </Frame>
                            <!-- Кнопка Назначить доступ -->
                            <Frame CornerRadius="32"
                                   BackgroundColor="#4B164C"
                                   Padding="0"
                                   HasShadow="False"
                                   WidthRequest="400"
                                   HeightRequest="71"
                                   HorizontalOptions="Center"
                                   Margin="0,10,0,0">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="AssignAccessClick" />
                                </Frame.GestureRecognizers>
                                <HorizontalStackLayout HorizontalOptions="Center"
                                                      VerticalOptions="Center"
                                                      Spacing="16"
                                                      Padding="0,0,8,0">
                                    <Image Source="assign_access.png"
                                           HeightRequest="32"
                                           WidthRequest="32"
                                           VerticalOptions="Center" />
                                    <Label Text="Назначить доступ"
                                           FontSize="32"
                                           FontFamily="Inter"
                                           TextColor="White"
                                           VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </Frame>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="1" Spacing="20">
                            <Frame WidthRequest="326" HeightRequest="70" BackgroundColor="White" CornerRadius="32" Padding="32,0" HasShadow="False" BorderColor="#E0E0E0">
                                <Entry x:Name="FirstNameEntry"
                                       Placeholder="Имя"
                                       FontSize="22"
                                       FontFamily="Inter"
                                       TextColor="Black"
                                       PlaceholderColor="#4A4A4A"
                                       VerticalOptions="Center"
                                       BackgroundColor="Transparent" />
                            </Frame>
                            <Frame WidthRequest="326" HeightRequest="70" BackgroundColor="White" CornerRadius="32" Padding="32,0" HasShadow="False" BorderColor="#E0E0E0">
                                <Entry x:Name="LastNameEntry"
                                       Placeholder="Фамилия"
                                       FontSize="22"
                                       FontFamily="Inter"
                                       TextColor="Black"
                                       PlaceholderColor="#4A4A4A"
                                       VerticalOptions="Center"
                                       BackgroundColor="Transparent" />
                            </Frame>
                            <!-- Кнопка Блокировать доступ -->
                            <Frame CornerRadius="32"
                                   BackgroundColor="#4B164C"
                                   Padding="0"
                                   HasShadow="False"
                                   WidthRequest="400"
                                   HeightRequest="71"
                                   HorizontalOptions="Center"
                                   Margin="0,10,0,0">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="BlockAccessClick" />
                                </Frame.GestureRecognizers>
                                <HorizontalStackLayout HorizontalOptions="Center"
                                                      VerticalOptions="Center"
                                                      Spacing="16"
                                                      Padding="0,0,8,0">
                                    <Image Source="block_access.png"
                                           HeightRequest="32"
                                           WidthRequest="32"
                                           VerticalOptions="Center" />
                                    <Label Text="Блокировать доступ"
                                           FontSize="32"
                                           FontFamily="Inter"
                                           TextColor="White"
                                           VerticalOptions="Center" />
                                </HorizontalStackLayout>
                            </Frame>
                        </HorizontalStackLayout>
                    </Grid>

                    <!-- Кнопки действий -->
                    <HorizontalStackLayout Grid.Row="4" Spacing="20" HorizontalOptions="Start" Margin="50,0,0,0">
                        <!-- Кнопка Добавить -->
                        <Frame CornerRadius="32"
                               BackgroundColor="#4B164C"
                               Padding="0"
                               HasShadow="False"
                               WidthRequest="280"
                               HeightRequest="71"
                               Margin="0,0,0,0">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="AddUserClick" />
                            </Frame.GestureRecognizers>
                            <HorizontalStackLayout HorizontalOptions="Center"
                                                  VerticalOptions="Center"
                                                  Spacing="16"
                                                  Padding="0,0,8,0">
                                <Image Source="add_user.png"
                                       HeightRequest="32"
                                       WidthRequest="32"
                                       VerticalOptions="Center" />
                                <Label Text="Добавить"
                                       FontSize="32"
                                       FontFamily="Inter"
                                       TextColor="White"
                                       VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Frame>
                        <!-- Кнопка Удалить -->
                        <Frame CornerRadius="32"
                               BackgroundColor="#4B164C"
                               Padding="0"
                               HasShadow="False"
                               WidthRequest="280"
                               HeightRequest="71"
                               Margin="0,0,0,0">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="DeleteUserClick" />
                            </Frame.GestureRecognizers>
                            <HorizontalStackLayout HorizontalOptions="Center"
                                                  VerticalOptions="Center"
                                                  Spacing="16"
                                                  Padding="0,0,8,0">
                                <Image Source="delete_user.png"
                                       HeightRequest="32"
                                       WidthRequest="32"
                                       VerticalOptions="Center" />
                                <Label Text="Удалить"
                                       FontSize="32"
                                       FontFamily="Inter"
                                       TextColor="White"
                                       VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Frame>
                    </HorizontalStackLayout>
                </Grid>
            </Frame>
        </Grid>
    </ScrollView>
</ContentPage>